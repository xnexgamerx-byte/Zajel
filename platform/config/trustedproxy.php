<?php

return [
    /*
    | من يُصدَّق فيما يقوله عن الزائر (X-Forwarded-For وProto).
    |
    | فارغٌ على خادمنا (deploy/): Caddy يسلّم PHP عنوان الزائر نفسه، وتصديق
    | الرؤوس حينها يترك أيّ زائرٍ يكتب عنواناً مخترعاً فيتجاوز حدّ محاولات
    | الدخول والتتبّع.
    |
    | و'*' خلف وكيلٍ لا يُوصَل التطبيق إلا عبره (Railway): بدونه يبدو كل
    | الزوّار عنواناً واحداً — عنوان الوكيل — فتُقفَل محاولات الدخول على
    | الجميع معاً بعد أخطاء قليلة.
    */
    'proxies' => env('TRUSTED_PROXIES'),
];
